#ifndef GCD_LCM_H
#define GCD_LCM_H

namespace dima {
	//! Calculates the greates common divisor of the given parameters
	unsigned gcd(unsigned u, unsigned v);
	//! Calculates the greates common divisor of the given parameters
	unsigned lcm(unsigned u, unsigned v);
	
}

#endif // GCD_LCM_H
